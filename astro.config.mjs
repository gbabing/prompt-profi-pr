import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://prompt-profi.babing-media.de',
  // Falls du später doch zu Subverzeichnis wechselst:
  // base: '/prompt-profi',
});
